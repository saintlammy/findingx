# Archived design directions

Superseded by the landing page in `src/pages/index.astro`, which implements the
Claude Design comp "Finding X - Landing Page" (the Crossing direction carried
into the official BuildersCabal palette).

- `crossing.astro` - Direction C, the exploratory version. Used the unofficial
  violet/lime palette.
- `Switcher.astro` - the A/C design preview toggle.

Kept for reference only. They are outside `src/pages/`, so they no longer build
as routes. Safe to delete once the new page is signed off.
